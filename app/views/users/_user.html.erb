<li>
  <%= gravatar_for(user) %>
  <%= link_to(user.name, user_path(user)) %>
  <% if current_user.admin? && !current_user?(user) %>
    <%= link_to("Delete", user_path(user), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger delete-user") %>
  <% end %>
</li>